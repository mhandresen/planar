name: Self diagram

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  diagram:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: ./
        with:
         plan: docs/examples/plan.json
         output: .planar/diagram.svg
         theme: dark